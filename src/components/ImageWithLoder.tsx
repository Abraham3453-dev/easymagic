import React from 'react';
import { View } from 'react-native';
export interface ImageWithLoaderProps {}
const ImageWithLoader = ({}: ImageWithLoaderProps) => {
    return (
        <View></View>
    )
}

export default ImageWithLoader;